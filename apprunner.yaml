version: 1.0
runtime: nodejs18
build:
  commands:
    build:
      - cd next-hire-backend && npm ci && npm run build
run:
  runtime-version: 18
  command: cd next-hire-backend && npm run start
  network:
    port: 5001
  env:
    - name: PORT
      value: "5001"
